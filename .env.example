###############################################################################
#                       General Configuration                                 #
###############################################################################

NODE_ENV=development           # Environment: development, production, staging
PROJECT_SLUG=daas              # Unique project identifier (used for Docker)
ENVIRONMENT=development        # Docker environment (relates to Dockerfiles)
USE_TRAEFIK=yes                # Enable Traefik reverse proxy (yes/no)
DOMAIN=localhost               # Root domain for the application

###############################################################################
#                       Database Configuration                                #
###############################################################################

DB_HOST=postgres               # Database host
DB_PORT=5555                   # Database port
DB_NAME=strapi                 # Database name
DB_USER=postgres               # Database user
DB_PASSWORD=postgres           # Database password

## DB SPECIFICS (Postgres) - Redundant, consider removing if DB_* variables suffice
POSTGRES_USER=postgres         # Postgres user (consider removing)
POSTGRES_PASSWORD=postgres     # Postgres password (consider removing)
POSTGRES_DB=strapi             # Postgres database (consider removing)
POSTGRES_PORT=5555             # Postgres port (consider removing)

###############################################################################
#                       Domain and Port Configuration                         #
###############################################################################

DOMAINNAME=localhost           # Main domain name
FRONTEND_DOMAINNAME=localhost  # Frontend domain
BACKEND_DOMAINNAME=localhost   # Backend domain
BACKEND_PORT=3030                # Backend port
FRONTEND_PORT=4040               # Frontend port

###############################################################################
#                       Notes                                                 #
###############################################################################
# - Ensure all necessary services (Postgres, Traefik) are running.
# - Update domain names to match your actual deployment environment.
# - For production, use strong, unique passwords.